A browsing app that gets a URL, reads its contents, and returns it.

<Accordion title="Browserbase load tool">
Load webpages url in a headless browser using browserbase and return the contents.
</Accordion>

