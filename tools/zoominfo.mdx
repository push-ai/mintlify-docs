ZoomInfo is a multiplatform operating system that revenue teams use to deliver business growth.

<Accordion title="Contact Enrich">
Use this endpoint to match contact information based on input values and return the desired output fields in the response. use contact enrich outputs to retrieve the list of available output fields.returns a list of news articles from zoominfo's data which meet the specified search criteria. all inputs are optional, but you must use at least one input parameter to generate a successful response.
</Accordion>

<Accordion title="Company Enrich">
Use this endpoint to match contact information based on input values and return the desired output fields in the response. use contact enrich outputs to retrieve the list of available output fields.returns a list of news articles from zoominfo's data which meet the specified search criteria. all inputs are optional, but you must use at least one input parameter to generate a successful response.
</Accordion>

<Accordion title="Scoop Search">
Returns a list of scoops from zoominfo's data which meet the specified search criteria.
</Accordion>

<Accordion title="Location Enrich">
Use this endpoint to match contact information based on input values and return the desired output fields in the response. use contact enrich outputs to retrieve the list of available output fields.returns a list of news articles from zoominfo's data which meet the specified search criteria. all inputs are optional, but you must use at least one input parameter to generate a successful response.
</Accordion>

<Accordion title="Technology Enrich">
Use this endpoint to match contact information based on input values and return the desired output fields in the response. use contact enrich outputs to retrieve the list of available output fields.returns a list of news articles from zoominfo's data which meet the specified search criteria. all inputs are optional, but you must use at least one input parameter to generate a successful response.
</Accordion>

<Accordion title="Scoop Search Inputs">
Returns a list of fields you can use as input for the scoop search action.
</Accordion>

<Accordion title="Company Search">
Returns a list of companies from zoominfo's data which meet the specified search criteria.
</Accordion>

<Accordion title="Company Search Inputs">
Returns a list of fields you can use as input for the company search action.
</Accordion>

<Accordion title="Intent Search Inputs">
Returns a list of fields you can use as input for the intent action.
</Accordion>

<Accordion title="Contact Search Inputs">
Returns a list of fields you can use as input for the contact search action.
</Accordion>

<Accordion title="News Search">
Returns a list of news articles from zoominfo's data which meet the specified search criteria. all inputs are optional, but you must use at least one input parameter to generate a successful response.
</Accordion>

<Accordion title="News Search Inputs">
Returns a list of fields you can use as input for the news search endpoint.
</Accordion>

<Accordion title="Intent Search">
Use this endpoint to search for companies and recommended contacts based on intent data. use input values to return the desired output fields in the response. for charging purposes, each intent signal returned is counted as a record.
</Accordion>

<Accordion title="Contact Search">
Returns a list of contacts from zoominfo's data that meet the specified search criteria.
</Accordion>

